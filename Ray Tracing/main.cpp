#include <iostream>
#include "scene.h"
//may need a raygen program that computes pixel colors
// need to create an OptiX pipeline
// need to create a frame buffer and launch the raygen

using namespace std;

int main() {
    render();
    return 0;
}
